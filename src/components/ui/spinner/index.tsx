import React, { Component } from 'react';

export default class Spinner extends Component<Props, IState> {
  render = () => (<div>Spinner...</div>);
}

interface IProps {}
interface IState {}

type Props = IProps;